# Variables de entorno para DESARROLLO LOCAL
# Para desarrollo, usamos el PROXY de Vite configurado en vite.config.js
# El proxy redirige /api a https://localhost:3443

# Configuración para conectar al backend (comentado para usar proxy)
# VITE_API_URL=http://localhost:3000
